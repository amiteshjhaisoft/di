# requirements.txt — LangChain RAG Streamlit app
# Python 3.10+ recommended

# --- App / UI ---
streamlit>=1.36,<1.41

# --- LangChain core + integrations ---
langchain>=0.2.10,<0.3.0
langchain-community>=0.2.10,<0.3.0
langchain-anthropic>=0.1.20,<0.2.0

# --- Vector store ---
chromadb>=0.5.3,<0.6.0

# --- Embeddings ---
sentence-transformers>=2.6.1,<3.0.0

# --- Loaders / parsing ---
pypdf>=4.2.0,<6.0.0
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.2,<4.0.0
beautifulsoup4>=4.12.2,<5.0.0
lxml>=5.1.0,<6.0.0

# --- LLM SDK (for Claude) ---
anthropic>=0.34.0,<0.40.0

# --- Data stack ---
pandas>=2.2.1,<3.0.0
numpy>=1.26,<3.0

# Optional (speedups / utilities)
# hnswlib>=0.8.0           # optional: faster ANN for Chroma (if available for your platform)
# tiktoken>=0.7,<0.8       # optional: token utilities
